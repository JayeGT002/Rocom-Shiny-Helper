# 洛克王国赛季异色属性池匹配器 — 需求实现文档

> 版本：v2.7  
> 部署目标：GitHub Pages  
> 技术栈：HTML5 + CSS3 + 原生 JavaScript（ES6+）

---

## 一、项目概述

### 1.1 背景
洛克王国手游每个赛季会推出一批「异色精灵」，玩家需要通过属性池刷取或赛季机制获取。本工具帮助玩家：
- 快速筛选目标精灵
- 识别哪些精灵可以通过属性池/赛季机制**定向刷取**
- 匹配对应属性的高星光值精灵，优化刷取效率

### 1.2 核心功能
| 功能模块 | 说明 |
|---|---|
| 赛季切换 | 支持 S1~S4 及未来赛季，通过下拉框切换 |
| 赛季机制提示 | 顶部机制框 = `noticeTitle` 标题 + 可折叠正文（`description` 按空行分段，**默认折叠**）；底色 `#B3BFD3`、深色文字（#1e2a42/#16203a） |
| 精灵筛选 | 按赛季定义的分组（如金月/赤月、赛季奇遇/常驻异色）**单选**目标精灵（最多一只，可取消） |
| 分类切换 | 配置双分类的赛季（如 S4）可在「赛季机制」与「赛季奇遇/常驻异色」间一键切换分组方式 |
| 属性筛选 | 单选属性（默认「全部」），选项仅含当前赛季精灵包含的属性；筛选后不可命中该属性的精灵置灰不可选，推荐区只显示该属性的属性池 |
| 命中属性 | 实时计算已勾选精灵涉及的所有属性，并入「推荐属性池」区块顶部展示 |
| 可定向识别 | 识别选中精灵可经 `directable.pool`（属性池直接定向）或 `directable.mechanism`（赛季机制近似定向）获得的属性 |
| 高星光值推荐 | 按命中属性匹配高星光值精灵，按属性分组、星光值降序排列 |
| 分组头定向 tag | 每个属性池分组头展示 tag：可定向池 = `[属性池定向]`（橙）或 `[赛季机制定向]`（底色 `#71C2F9`，文字 `#3D4F1F` = BFDE7B 加深，可内嵌 金月/赤月 子标）；不可定向池 = `[不可定向]`（灰）；无绿色「可定向」tag，无折叠框，全部平铺展示 |
| 筛选分类并列 | 精灵筛选区若为两个分类（如金月/赤月、赛季奇遇/常驻异色）则**左右并列**展示，每个分类内精灵卡片**一行 3 张** |
| 精灵卡片底色 | 精灵/推荐卡图片区**无底色直铺**：透明 PNG 立绘直接叠在卡片背景上，选中时透出卡片高亮色 |
| 地区形态名称 | 名称含括号（地区形态）时**括号另起一行**：主名一行 + 括号行小字灰色（12px/#9aa0a6）；无括号卡片保留同高度空白副行，保证卡片等高 |
| 地区形态提示 | 配置 `family` 字段的地区形态精灵（如刺轮砣双形态）独立为筛选选项；选中其一后显示提示条，说明这些形态为同一精灵的地区形态、相互独立、需特殊方式刷取 |
| 图片占位 | 精灵图片加载成功后隐藏「暂无图片」占位，仅真正无图的精灵保留占位 |
| 常规/异色交替 | S1~S3 精灵卡片图每 5 秒在常规图与异色图间交替（容器内两层图**交叠淡入淡出**，CSS transition 2.4s，无硬切；`data/yise_images.json` 清单驱动，S4 等 wiki 更新后补充） |
| 阶段信息 | 推荐属性池精灵卡以**浅蓝胶囊**（`#dff1ff`/`#0b6bcb`）展示阶数（如「二阶」「三阶」，首领化形态不计入）；阶段图已移除 |
| 推荐卡版式 | 推荐属性池精灵卡：120px 透明立绘 + 两行名称 + 「第一属性：图标+属性」行 + 第二行（星光值 + 阶数浅蓝胶囊）；分组头字体 18px、tag 12px 圆角胶囊 |

---

## 二、数据结构

### 2.1 赛季列表 `data/seasons.json`

```json
{
  "seasons": [
    {"id": "S4", "name": "月涌狂想"},
    {"id": "S3", "name": "铅字幻梦"},
    {"id": "S2", "name": "狂欢怪谈"},
    {"id": "S1", "name": "暗夜拾光"}
  ]
}
```

### 2.2 赛季数据 `data/S{x}.json`

```json
{
  "season": "S4",
  "name": "月涌狂想",
  "noticeTitle": "赛季奇遇「星陨流光」",
  "description": "本赛季奇遇精灵将分为「金月奇遇精灵」与「赤月奇遇精灵」两大主题（……段落以空行分隔，完整文案见 data/S4.json）",
  "filterGroups": [
    {
      "key": "gold",
      "name": "金月奇遇",
      "color": "#D4A843",
      "badgeText": "金月",
      "badgeColor": "#D4A843"
    },
    {
      "key": "crimson",
      "name": "赤月奇遇",
      "color": "#C0392B",
      "badgeText": "赤月",
      "badgeColor": "#C0392B"
    }
  ],
  "sprites": [
    {
      "name": "章脑丸",
      "attr1": "光",
      "attr2": "水",
      "group": "crimson",
      "directable": {
        "pool": [],
        "mechanism": ["光", "水"]
      }
    },
    {
      "name": "小黑猫",
      "attr1": "普通",
      "attr2": "",
      "group": "gold",
      "directable": {
        "pool": ["普通"],
        "mechanism": []
      }
    }
  ]
}
```

**字段说明：**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `season` | string | 是 | 赛季ID，与文件名对应 |
| `name` | string | 是 | 赛季名称 |
| `noticeTitle` | string | 否 | 赛季机制提示框**标题**（如「赛季奇遇『星陨流光』」），缺省回退为 `{name} 赛季机制` |
| `description` | string | 否 | 赛季机制正文，有值才显示顶部提示框；段落以空行 `\n\n` 分隔，提示框**默认折叠**、点击标题展开 |
| `filterGroups` | array | 是 | 筛选栏分组配置，决定页面布局 |
| `filterGroups[].key` | string | 是 | 分组标识，精灵通过 `group` 字段匹配 |
| `filterGroups[].name` | string | 是 | 筛选栏标题 |
| `filterGroups[].color` | string | 是 | 筛选栏主题色（边框、标题、背景） |
| `filterGroups[].badgeText` | string | 否 | 机制子标文字，如"金月""赤月"。有值则该组精灵的「赛季机制定向」tag 内嵌该子标 |
| `filterGroups[].badgeColor` | string | 否 | 机制标签背景色，默认使用 `color` |
| `categoryGroups` | array | 否 | **可选**，第二种分类方式（赛季奇遇/常驻异色）的分组配置，结构同 `filterGroups`。配置后页面在赛季机制说明下方显示「分类方式」切换开关 |
| `sprites` | array | 是 | 精灵列表 |
| `sprites[].name` | string | 是 | 精灵名称 |
| `sprites[].attr1` | string | 是 | 第一属性 |
| `sprites[].attr2` | string | 否 | 第二属性，单属性留空 `""` |
| `sprites[].group` | string | 是 | 所属分组，对应 `filterGroups[].key`（赛季机制分类） |
| `sprites[].group2` | string | 否 | **可选**，第二种分类方式下的分组归属，对应 `categoryGroups[].key` |
| `sprites[].family` | string | 否 | **可选**，地区形态标记（如刺轮砣的上下弦形态共用 `"刺轮砣"`），用于地区形态提示 |
| `sprites[].form` | string | 否 | **可选**，地区形态名（如 `"上弦的样子"`），与 `family` 配合标识形态 |
| `sprites[].previewImage` | bool | 否 | **可选**，为 `true` 时该精灵使用预览立绘：前端取图 `images/S{赛季}/{名称}（预览版）.png`（v2.7，S4 8 只用预览图） |
| `sprites[].directable` | object | 否 | 定向刷取标记 |
| `sprites[].directable.pool` | array | 否 | 可直接定向的属性池列表。该属性池中只有这一只精灵 |
| `sprites[].directable.mechanism` | array | 否 | 可通过赛季机制近似定向的属性池列表。该属性池中，该精灵所在分组唯一 |

> **双分类机制**：`filterGroups` + `sprites[].group` 为「赛季机制分类」（如金月/赤月）；
> `categoryGroups` + `sprites[].group2` 为「赛季奇遇/常驻异色分类」（season/resident）。
> 前端通过开关切换分类方式，仅当赛季配置了 `categoryGroups` 时才显示开关，其他赛季无影响。

### 2.3 高星光值精灵池 `data/high_value_sprites.json`

```json
{
  "sprites": [
    {"name": "白金独角兽", "attr": "光", "value": 60, "stages": ["小独角兽", "白金独角兽", "彩虹独角兽"]},
    {"name": "星云旅者", "attr": "翼", "value": 80, "stages": ["星云旅者"], "excludeSeasons": ["S1", "S2"]}
  ]
}
```

| 字段 | 说明 |
|---|---|
| `name` | 精灵名称（最高阶段名称，地区形态精灵使用原始名称） |
| `attr` | 所属属性（**仅第一属性**；推荐卡图下以 `属性图标+名称` chip 展示，v2.3 起不再使用「第一属性」徽章文案） |
| `value` | 星光值 |
| `stages` | **可选**，进化链各阶段名称（按进化顺序，含本体），用于推荐卡阶段数 tag（如「三阶」）；**首领化形态不计入**（见注意事项） |
| `excludeSeasons` | **可选**，不出现在这些赛季的推荐中（如星云旅者 `["S1","S2"]` 表示仅 S3 起推荐） |

> **地区形态**：拥有地区形态的精灵在高星光值池中**仅保留一个形态**、使用原始名称（无括号）：
> 蹦蹦花 → 保留海神球形态；乌拉塔 → 保留极昼的样子；梦悠悠 → 保留穿旧睡衣的样子；
> 化蝶 → 保留平常的样子；星光狮 → 保留星光能量的样子。
> 各形态第一属性与星光值均与本体一致，仅展示名称不同。

### 2.4 属性图标与颜色（`images/attrs/`）

属性图标从 [洛克王国手游WIKI - 精灵图鉴](https://wiki.biligame.com/rocom/精灵图鉴) 提取，
页面中属性标签、可定向圆点、推荐分组图标均使用该图片（198×198 圆形徽章 + 白色符号）。

**提取方式**（via MediaWiki API）：

```
https://wiki.biligame.com/rocom/api.php?action=query&list=allimages&aiprefix=图标 宠物 属性&ailimit=50&format=json
```

图片命名规范：`图标 宠物 属性 {属性名}.png` → 本地存放为 `images/attrs/{属性名}.png`。
> 注意：wiki 主站有 EdgeOne 防护，直接 curl 会被拦截（HTTP 567）；WebFetch 与图片 CDN
> `patchwiki.biligame.com`（curl -k）可用。

**颜色代码**：每张图标经 PIL 量化 + 出现频次统计提取主色，硬编码于前端 `attrColors`：

| 属性 | 色值 | 属性 | 色值 |
|---|---|---|---|
| 普通 | `#3080B0` | 毒 | `#B060E0` |
| 草 | `#40B070` | 虫 | `#90C020` |
| 火 | `#D05020` | 武 | `#F09030` |
| 水 | `#60A0F0` | 翼 | `#30C0C0` |
| 光 | `#40C0F0` | 萌 | `#F070A0` |
| 地 | `#907030` | 幽 | `#9040E0` |
| 冰 | `#50A0D0` | 恶 | `#C04070` |
| 龙 | `#E04060` | 机械 | `#40C0A0` |
| 电 | `#E0C000` | 幻 | `#90A0F0` |

### 2.5 精灵图片（`images/S{赛季}/{精灵名}.png`）

精灵本体图从 wiki 各精灵页面提取，`fetch_images.py` 采用**两级策略**：

**策略一（主）：`action=parse&prop=text` 提取 JL 立绘**

```
https://wiki.biligame.com/rocom/api.php?action=parse&page={精灵名}&prop=text&format=json
```

页面 HTML 中匹配 `src="https://patchwiki.biligame.com/images/rocom/thumb/{a}/{ab}/{hash}.png/NNNpx-JL_xxx.png"`，
推导原图 URL：

```
原图: https://patchwiki.biligame.com/images/rocom/{a}/{ab}/{hash}.png
```

**策略二（兜底）：`action=parse&prop=images` 列嵌入文件**

策略一未命中（部分页面 HTML 不含 `JL_` 缩略图或接口被拦）时，改为列出页面嵌入的全部
`File:` 标题，筛选含 `JL_` 的图片，再通过 `action=query&prop=imageinfo` 取真实 URL 下载。

- 图片为 1024×1024 精灵立绘，命名 `images/S{赛季}/{精灵名}.png`
- 高星光值精灵图存 `images/high_value/{精灵名}.png`（按**最高阶段名称 / 原始名称**命名）
- 地区形态精灵仅保留一个形态、使用原始名称（如高星光值池中 `化蝶.png` / `蹦蹦花.png`）
- **星光值图标**：`images/star_value.png`（48×48，全站通用，从 `File:信息图标 星光值.png` 经 imageinfo 获取）
- **例外**：wiki 上为**空壳页面**（未收录）的精灵无立绘，页面保留"暂无图片"占位
- 抓取需注意 wiki 主站 EdgeOne 防护（HTTP 567 / 挑战页）：请求需带浏览器 UA、多重重试、
  限速（单请求间隔 ≥2s）；图片 CDN `patchwiki` 稳定。另注意推导原图 URL 时**不得重复拼接**
  `.png` 后缀（`hash.png` 本身已含扩展名，否则 404）

> **名称约定**：赛季 JSON（筛选器）统一使用**第一阶段名称**（如 粉星仔/守夜烛/牵线木偶），
> 高星光值推荐池使用**最高阶段名称**（如 粉耳星兔/流明坎德拉/帅帅魔偶），两者是不同概念，
> 前端图片分别存放在 `images/S{赛季}/` 与 `images/high_value/`，互不覆盖。

### 2.6 异色图（`images/S{赛季}/{精灵名}_异色.png`）

S1~S3 精灵的**异色立绘**由 `fetch_yise.py` 抓取：`action=parse&prop=images` 列出页面嵌入文件，
筛选 `JL_*_yise.png`（与主图同基底名），经 `action=query&prop=imageinfo` 取原图 URL 下载。

- 命名：`images/S{赛季}/{精灵名}_异色.png`（主图同名 + `_异色` 后缀）
- **异色图覆盖情况**（以各赛季数据精灵为准，截止 v2.2）：
  - S1：全覆盖（16/16；含名称更正为「嗜光嗡嗡」的精灵，原数据/名单误作「噬光嗡嗡」）
  - S2：全覆盖（16/16）
  - S3：全覆盖（18/18；v2.2 补抓 伊贝儿、栗鼠、海盔虫、海盔虫（磨损的样子））
  - S4：全部 17 只均**暂无**异色图（S4 暂无异色，等 wiki 更新；其中 8 只连本体图也暂无：星星眼、玳龟、章脑丸、幽铃、布灵、未完虫、量风碗、小浣蛋）
- 前端卡片图每 **5 秒**在常规图与异色图间**交替显示**（`data/yise_images.json` 清单驱动，仅清单内精灵启用，
  避免对不存在异色图的精灵产生 404 请求噪音）。v2.1 起为**双图交叠淡入淡出**：卡片图片容器叠加两层 `<img>`，
  各自预载常规图与异色图，到点后仅切换两层的 `opacity`（CSS `transition: opacity 2.4s ease-in-out`），两图重叠过渡无硬切。

### 2.7 进化阶段数据（`stages` 字段）

高星光值精灵的进化链各阶段名称由 `fetch_stages.py` 抓取（`action=parse&prop=text` 解析
`sprite-evolve-section`），写入 `data/high_value_sprites.json` 的 `stages` 数组，供前端显示阶段数 tag。

- **首领化形态不计入**：URL 含 `_shouling` 的阶段自动跳过（不算正常进化阶段），
  因此如 恶魔狼 只有「恶魔狼」一阶（恶魔狼王为首领化），迷迷箱怪为「小箱怪、迷迷箱怪」二阶
- **仅统计阶段数**：v2.0 起不再展示阶段图，`images/high_value/` 仅存放精灵本体图（最高阶段名），
  阶段名不再单独存图

### 2.8 异色图清单 `data/yise_images.json`

```json
{
  "S1": ["治愈兔", "恶魔狼", "..."],
  "S2": ["小丑豆豆", "..."],
  "S3": ["十字蝌蚪", "..."]
}
```

- 由 `fetch_yise.py` 抓取结束后自动生成（遍历 `images/S{赛季}/` 下 `*_异色.png` 得到）
- 前端交替逻辑完全由该清单驱动：**未来赛季更新只需补充清单**，无需改前端代码

---

## 三、页面布局与交互

### 3.1 整体布局

> 布局演进：v2.0 起页面整体缩放至 **90%**（`body { zoom: 0.9 }`）；右侧列表栏已**删除**（内容后续迁移至独立页面）；
> 命中属性并入推荐区顶部。v2.1/v2.2：分类并列、无底色直铺、名称括号双行、异色双图交叠（5s/2.4s）。
> v2.4/v2.5：
> - 结果区块更名**「推荐属性池」**，独立「可定向刷取精灵」区块与「其他属性池（无法定向）」折叠框均已**删除**，全部属性池平铺；
> - 分组头定向 tag：可定向池 = `[属性池定向]`（橙）/`[赛季机制定向]`（蓝，内嵌 **金月/赤月** 子标）；不可定向池 = `[不可定向]`（灰）；分组头字体 18px、tag 12px；
> - 推荐精灵卡版式：立绘 + 两行名称 + `第一属性：图标+属性` 单行 + 第二行（星光值 + **浅蓝胶囊**阶数）。

```
┌──────────────────────────────────────────────────────────────┐
│  标题                                            [赛季选择▼]  │  ← Header
├──────────────────────────────────────────────────────────────┤
│  [赛季机制说明提示框（可选）]                                  │
│  [分类方式：|赛季机制|赛季奇遇/常驻异色]                       │
│  [属性筛选：|全部|普通|光|机械|幻|...]                        │
│  ┌────────────┐ ┌────────────┐                               │
│  │ 金月奇遇     │ │ 赤月奇遇     │   ← 两个分类左右并列，每个    │
│  │ 每行3张卡片   │ │ 每行3张卡片   │     分类内卡片一行 3 张      │
│  └────────────┘ └────────────┘                               │
│  [地区形态提示：刺轮砣双形态相互独立...]                       │
│  推荐属性池（每行 5 张卡片，全部平铺无折叠）                   │
│  ┌────────────────────────────┐                              │
│  │ [命中属性：光][水][幽]...    │   ← 并入推荐区顶部            │
│  ├────────────────────────────┤                              │
│  │ ●光 (3只) [赛季机制定向|赤月] │  ← 分组头 tag：可内嵌金月/赤月 │
│  │ [图]名称                    │                              │
│  │ 第一属性：◆光              │                              │
│  │ ⭐80 [三阶]浅蓝胶囊          │                              │
│  │ ●恶 (2只) [不可定向]        │   ← 不可定向池灰 tag 平铺      │
│  └────────────────────────────┘                              │
└──────────────────────────────────────────────────────────────┘
```

### 3.2 交互逻辑

#### 赛季切换
1. 用户从下拉框选择赛季
2. 前端通过 `fetch()` 加载对应 `data/S{x}.json`
3. 清空已勾选状态，重置分类方式为「赛季机制」，重新渲染筛选区
4. 根据 `filterGroups` 动态生成筛选栏（支持 1~3 栏自适应）

#### 分类方式切换（开关）
1. 仅当赛季 JSON 配置了 `categoryGroups` 时，赛季机制说明下方显示「分类方式」开关
2. 两种分类方式：「赛季机制」（`filterGroups` + `sprites[].group`）与「赛季奇遇/常驻异色」（`categoryGroups` + `sprites[].group2`）
3. 切换时清空已勾选精灵，按新分组重新渲染筛选栏
4. 「赛季机制定向」tag 内嵌的机制子标（如 S4 金月/赤月）始终按 `filterGroups.badgeText/badgeColor` 显示

#### 精灵勾选（单选）
1. 精灵卡片采用**单选**：点击未选中精灵时自动替换已有选择，点击已选中精灵可取消选择
2. `selectedSprites` 为 Set，最多包含 1 个元素；变更后调用 `syncSelectionUI()` 同步所有卡片选中视觉
3. 触发 `updateResults()` 重新计算所有结果区域

#### 属性筛选（单选）
1. 「属性筛选」开关位于分类方式下方，选项 = 当前赛季精灵包含的所有属性（去重，按属性色值表固定顺序排列）+「全部」默认项
2. 单选原则：同一时间只能选择 1 个属性，「全部」表示不过滤；切换赛季时重置为「全部」
3. 属性筛选按钮（含「全部」）与页面其他标题/按钮一致，使用**页面字体**（`--font-game`）
4. 选中属性 X 后：
   - 清空已选精灵，重新渲染筛选区
   - 精灵卡片中**不可命中 X** 的置灰不可选（`canHitAttr`：自身属性包含 X，或 `directable.pool`/`directable.mechanism` 包含 X）
   - 推荐列表只显示属性 X 的高星光值属性池（`s.attr === X` 过滤）

#### 地区形态提示
1. 精灵配置 `family` 字段时标记为「地区形态」（如刺轮砣的「上弦的样子」「下弦的样子」），在筛选中仍**独立成两个选项**
2. 选中任一形态后，筛选区下方显示地区形态提示条（标题「地区形态提示」）：
   - 列出同 `family` 的其他形态（`family` 相同且 `name` 不同的精灵）
   - 说明这些形态为同一精灵的地区形态、**相互独立**（非同一只精灵），需特殊方式刷取
   - 具体刷取方式待补充，当前文案不描述具体方式
3. 未选中精灵或选中无 `family` 字段的精灵时，提示条隐藏
4. 前端完全通用：新增地区形态精灵只需在 JSON 配置 `family` + `form` 字段

#### 图片占位符
1. 精灵卡片（筛选卡/推荐卡）的图片容器默认显示「暂无图片」占位
2. 图片 `load` 成功后为容器添加 `has-img` class，CSS 隐藏占位文字；加载失败则保留
3. 效果：已更新图片的精灵不再显示占位，仅真正无图的精灵（如 S4 空壳页 8 只）保留

#### 常规/异色图交替
1. 前端加载 `data/yise_images.json` 异色图清单，仅清单内的精灵注册交替（每 **5 秒** `setInterval` 触发）
2. v2.1 起为**双图交叠淡入淡出**：`setupSwap` 在图片容器内追加一张同尺寸 `<img>`（预载异色图、`opacity:0`）并给容器加 `sw-ready`；
   `setInterval` 到点后仅切换两层 `opacity`（CSS `transition: opacity 2.4s ease-in-out`），两层图重叠过渡、**不再替换 `src`**，无硬切/闪烁
3. 清单驱动保证不请求不存在的异色图，无 404 噪音；S4 暂无清单，等 wiki 更新后补充
4. 容器 `sw-ready` 中两层图均 `position:absolute` 铺满，占位符逻辑（`has-img`）仍以主图加载状态为准

#### 推荐属性池精灵卡与分组头 tag（v2.4/v2.5）
1. 推荐卡版式（自上而下）：120px 透明立绘 → 两行名称（`nameBlock`）→ `第一属性：图标+属性` 单行（`.rc-attr`，金色系小字）→ 第二行（`.rc-foot`：星光值图标+数值 与 **浅蓝胶囊**（`#dff1ff`/`#0b6bcb`）阶数气泡）
2. 阶数胶囊（如「二阶」「三阶」，中文数字，由 `stages` 数组长度计算）仅在 `stages` 非空时显示；首领化形态（`_shouling`）不计入
3. 已删除历史样式：`first-attr-badge`（黄徽章）、`rc-row`、`value-tag`、`stage-tag`、`rc-meta/rc-chip`
4. **分组头 tag（v2.5）**：绿色「可定向」tag 已删除。可定向池：命中 `directable.pool` 显示 `[属性池定向]`（橙）；命中 `directable.mechanism` 显示 `[赛季机制定向]`（蓝），其内**内嵌机制子标**（取 `filterGroups.badgeText`，如 S4 金月/赤月，按 `badgeColor` 着色）；不可定向池显示 `[不可定向]`（灰）。分组头字体 18px、count 13px、tag 12px 圆角胶囊
5. **不再折叠**：「其他属性池（无法定向）」折叠框已删除，全部属性池分组平铺展示（可定向池优先，其后为不可定向池）

#### 推荐赛季排除（excludeSeasons）
1. 高星光值精灵配置 `excludeSeasons` 时，当前赛季在该列表中的精灵**不进入推荐**
2. 星云旅者（翼 80）：S3 加入 → `["S1","S2"]`；帅帅魔偶（幻 60）：S2 加入 → `["S1"]`

#### 命中属性并入推荐区
1. v2.0 起删除独立的「命中属性」区块，改为并入「推荐属性池」区块顶部（`.recommend-hit-attrs`）
2. 选中精灵后推荐区标题下方展示「命中属性：」+ 属性标签列表（图标+属性名，按命中属性色值着色）
3. 命中属性为空（选中精灵无属性）时显示灰色「无」

---

## 四、核心算法

### 4.1 命中属性计算

```javascript
const hitAttrs = new Set();
selectedSprites.forEach(name => {
  const matches = currentSeasonData.sprites.filter(s => s.name === name);
  matches.forEach(s => {
    if (s.attr1) hitAttrs.add(s.attr1);
    if (s.attr2) hitAttrs.add(s.attr2);
  });
});
```

### 4.2 可定向识别（并入分组头 tag）

```javascript
// 属性池直接定向：该属性池中只有这一只精灵 → 由 JSON 中的 directable.pool 标记
// 赛季机制近似定向：该属性池中，该精灵所在分组唯一 → 由 JSON 中的 directable.mechanism 标记
// v2.5 起（无绿色「可定向」tag、无折叠）：
//   poolAttrs 命中属性 → 分组头 [属性池定向]（橙）
//   mechAttrs 命中属性 → 分组头 [赛季机制定向]（蓝，内嵌金月/赤月子标）
//   两者皆不命中      → 分组头 [不可定向]（灰）
```

### 4.3 推荐列表分组逻辑

```javascript
// 1. 分离可定向（pool/mech 命中）与不可定向的属性
const poolAttrs = new Set();
const mechAttrs = new Set();
selectedData.forEach(s => {
  const d = s.directable;
  if (!d) return;
  (d.pool || []).forEach(a => poolAttrs.add(a));
  if ((d.mechanism || []).length > 0) {
    (d.mechanism).forEach(a => mechAttrs.add(a));
    // mechTag = filterGroups 命中 s.group 的 {badgeText, badgeColor}（如 金月/赤月）
  }
});

// 2. 全部属性池分组平铺：可定向分组优先（分组头 [属性池定向]/[赛季机制定向]），
//    其余分组标 [不可定向]（灰）；无任何折叠框
```

---

## 五、赛季配置示例

### 5.1 S4 月涌狂想（金月/赤月机制 + 双分类）

```json
{
  "season": "S4",
  "name": "月涌狂想",
  "noticeTitle": "赛季奇遇「星陨流光」",
  "description": "本赛季奇遇精灵将分为「金月奇遇精灵」与「赤月奇遇精灵」两大主题（……完整文案见 data/S4.json，空行分段）",
  "filterGroups": [
    {"key": "gold", "name": "金月奇遇", "color": "#D4A843", "badgeText": "金月", "badgeColor": "#D4A843"},
    {"key": "crimson", "name": "赤月奇遇", "color": "#C0392B", "badgeText": "赤月", "badgeColor": "#C0392B"}
  ],
  "categoryGroups": [
    {"key": "season", "name": "赛季奇遇", "color": "#e65100"},
    {"key": "resident", "name": "常驻异色", "color": "#2e7d32"}
  ],
  "sprites": [
    {"name": "布灵", "attr1": "幻", "attr2": "光", "group": "gold", "group2": "season", "directable": {"pool": [], "mechanism": ["光"]}},
    {"name": "章脑丸", "attr1": "光", "attr2": "水", "group": "crimson", "group2": "season", "directable": {"pool": [], "mechanism": ["光", "水"]}},
    {"name": "小黑猫", "attr1": "普通", "attr2": "", "group": "gold", "group2": "resident", "directable": {"pool": ["普通"], "mechanism": []}},
    {"name": "刺轮砣（上弦的样子）", "attr1": "毒", "attr2": "萌", "group": "crimson", "group2": "resident", "family": "刺轮砣", "form": "上弦的样子", "directable": {"pool": ["毒"], "mechanism": []}},
    {"name": "刺轮砣（下弦的样子）", "attr1": "毒", "attr2": "萌", "group": "crimson", "group2": "resident", "family": "刺轮砣", "form": "下弦的样子", "directable": {"pool": ["毒"], "mechanism": []}}
  ]
}
```

> **S4 双分类归属（来源：`工作簿1.xlsx` Sheet1）**：
> - 赛季奇遇 `season`（8只）：章脑丸、量风碗、幽铃、未完虫、小浣蛋、星星眼、玳龟、布灵
> - 常驻异色 `resident`（8只）：小黑猫、刺轮砣、觅觅蝠、逗逗、小灵菇、芽眼魔、瑰眼仔、叮叮卯
>
> **刺轮砣地区形态**：拥有「上弦的样子」「下弦的样子」两个形态，在筛选中**独立成两个选项**，
> 但本质为同一精灵的地区形态，`family: "刺轮砣"` 标记。v1.8 起选中任一形态后，
> 页面显示标题为「地区形态提示」的提示条，说明二者为相互独立的精灵、需特殊方式刷取
> （具体刷取方式待补充）。

### 5.2 S1~S3（通用 season/resident + 第一阶段名称）

S1/S2/S3 数据均按《洛克王国精灵异色名单.csv》重建：分组统一为「赛季奇遇」(`season`) /
「常驻异色」(`resident`)，精灵统一使用**第一阶段名称**（S1 16 只、S2 16 只、S3 18 只）。

```json
{
  "season": "S3",
  "name": "铅字幻梦",
  "description": "",
  "filterGroups": [
    {"key": "season", "name": "赛季奇遇", "color": "#e65100"},
    {"key": "resident", "name": "常驻异色", "color": "#2e7d32"}
  ],
  "sprites": [
    {"name": "守夜烛", "attr1": "火", "attr2": "光", "group": "season"},
    {"name": "海盔虫", "attr1": "水", "attr2": "毒", "group": "resident", "family": "海盔虫"},
    {"name": "海盔虫（磨损的样子）", "attr1": "水", "attr2": "毒", "group": "resident", "family": "海盔虫", "form": "磨损的样子"},
    {"name": "地鼠（枯水期的样子）", "attr1": "地", "attr2": "", "group": "resident", "family": "地鼠", "form": "枯水期的样子"},
    {"name": "地鼠（储水时的样子）", "attr1": "地", "attr2": "", "group": "resident", "family": "地鼠", "form": "储水时的样子"}
  ]
}
```

> **名称对照（第一阶段 = 最高阶段）**：粉星仔 = 粉耳星兔（S1）、守夜烛 = 流明坎德拉（S3）、
> 牵线木偶 = 帅帅魔偶（S2）。筛选器用第一阶段名称；推荐高星光值池仍用最高阶段名称，互不影响。

### 5.3 S5 假设新机制（昼/夜）

```json
{
  "season": "S5",
  "name": "新赛季",
  "description": "S5新机制说明...",
  "filterGroups": [
    {"key": "day", "name": "昼之精灵", "color": "#F39C12", "badgeText": "昼", "badgeColor": "#F39C12"},
    {"key": "night", "name": "夜之精灵", "color": "#2C3E50", "badgeText": "夜", "badgeColor": "#2C3E50"},
    {"key": "resident", "name": "常驻异色", "color": "#2e7d32"}
  ],
  "sprites": [
    {"name": "新精灵A", "attr1": "光", "attr2": "", "group": "day", "directable": {"pool": [], "mechanism": ["光"]}},
    {"name": "新精灵B", "attr1": "幽", "attr2": "", "group": "night", "directable": {"pool": [], "mechanism": ["幽"]}}
  ]
}
```

> 前端代码**完全不需要修改**，只需上传新的 JSON 即可支持任意分组逻辑。

---

## 六、部署指南

### 6.1 GitHub Pages 部署

1. 在 GitHub 新建仓库 `rocom-shiny-tool`
2. 将 `index.html` 和 `data/` 目录推送到仓库
3. Settings → Pages → Deploy from a branch → `main` / `/(root)`
4. 等待 1-2 分钟后访问 `https://<用户名>.github.io/rocom-shiny-tool/`

### 6.2 本地预览

```bash
python -m http.server 8080
# 访问 http://localhost:8080
```

### 6.3 更新赛季数据

1. 修改/创建 `data/S{x}.json`
2. 如需新增赛季，同步更新 `data/seasons.json`
3. 推送至 GitHub，GitHub Pages 自动重新部署

---

## 七、注意事项

1. **刺轮砣（原称月亮砣）**：S4 数据已统一使用「刺轮砣」，精灵全部按照第一形态计算，如有问题请直接询问用户。
2. **飞飞钥 = 叮叮卯**：S4 数据中统一使用「叮叮卯」，属性为机械+虫
3. **混乱鱿彩（原称秩序鱿墨）**：名称已修正为「混乱鱿彩」，第一属性不变（幽），进化链为「墨鱿士 → 混乱鱿彩」二阶；`images/high_value/混乱鱿彩.png` 已重新抓取（wiki 新立绘，1024×1024）
4. **名称阶段约定**：赛季筛选器用**第一阶段名称**（粉星仔/守夜烛/牵线木偶），推荐高星光值池用**最高阶段名称**（粉耳星兔/流明坎德拉/帅帅魔偶），两者是不同概念，图片分别存放于 `images/S{赛季}/` 与 `images/high_value/`，不要互相覆盖。高星光值池中拥有地区形态的精灵（乌拉塔/梦悠悠/化蝶/星光狮/蹦蹦花）**仅保留一个形态、统一使用原始名称**（蹦蹦花/乌拉塔/梦悠悠/化蝶/星光狮），阶段名同样去掉括号后缀
5. **BASE_PATH**：若部署到子目录（如 `username.github.io/repo-name/`），需修改 `index.html` 顶部的 `BASE_PATH` 变量
6. **CORS**：本地直接打开 `index.html` 可能因 CORS 限制无法加载 JSON，建议通过本地服务器预览
7. **表述规范**：避免使用"100%命中""只有这一只"等绝对化表述，统一使用"可定向""大概率"
8. **进化阶级修正（v2.0）**：奇丽花（草）、罗隐（地）、高脚鹬（翼）均为**三阶**，`stages` 只保留前三个阶段（奇丽草/奇丽叶/奇丽花、阿米亚特/阿米樱/罗隐、小鹬/鄙目鹬/高脚鹬），后续首领化/进化形态（奇丽果、深渊罗隐、高帽鹬者）不计入
9. **布局约定（v2.0）**：页面整体 `zoom: 0.9` 缩放；筛选/可定向/推荐卡片 `repeat(5, minmax(0, 1fr))` 一行 5 个；右侧列表栏已删除（内容待迁移到独立新页面）；阶段图已全部移除，`images/high_value/` 仅保留 46 张精灵本体图
10. **名称更正（v2.2）**：S1 精灵实际名称为**嗜光嗡嗡**（数据原误作「噬光嗡嗡」），已同步更正 `data/S1.json`、`images/S1/` 文件名、《洛克王国精灵异色名单.csv》；v2.2 经 MediaWiki API 补抓异色图后，S1/S2/S3 异色图已**全部覆盖**（S1 16、S2 16、S3 18）
11. **卡片名称两行结构（v2.2）**：地区形态（含括号名称）卡片名称区 = 主名一行 + 括号行（12px 灰色 #9aa0a6）；无括号卡片留同名高位空白副行，保证卡片名称区等高
12. **白金独角兽阶数修正（v2.4）**：实际为**二阶**，`stages` 调整为 `["小独角兽","白金独角兽"]`（删除误入的「彩虹独角兽」）
13. **定向 tag 与折叠调整（v2.5）**：绿色「可定向」tag 删除；分组头仅显示 `[属性池定向]`（橙）/`[赛季机制定向]`（蓝，内嵌 金月/赤月 子标）/`[不可定向]`（灰）；「其他属性池（无法定向）」折叠框删除、全部平铺；分组头字体 18px、tag 12px
14. **机制提示框与定向配色（v2.6）**：顶部机制提示改为 `noticeTitle` 标题 + 可折叠正文（默认收起），底色 `#B3BFD3`、深色文字；`[赛季机制定向]` 底色 `#71C2F9`、文字 `#3D4F1F`（BFDE7B 加深色）
15. **S4 预览版立绘（v2.7）**：此前缺图的 8 只（章脑丸、量风碗、幽铃、未完虫、小浣蛋、星星眼、玳龟、布灵）使用预览立绘，文件复制为 `images/S4/{名称}（预览版）.png`，数据 `previewImage: true` 驱动前端取图；原图存放于 `images/S4赛季奇遇-预览版/` 作为来源备份

---

## 八、文件清单

```
.
├── index.html                      # 主页面（唯一入口）
├── fetch_images.py                 # 精灵/高星光值/星光值图标抓取脚本（wiki API → patchwiki CDN）
├── fetch_yise.py                   # S1~S3 异色图抓取脚本（生成 images/S{赛季}/*_异色.png + yise_images.json 清单）
├── fetch_stages.py                 # 高星光值精灵进化阶段抓取脚本（仅生成 stages 字段，阶段图已移除）
├── fonts/
│   └── MIANFEIZITI.ttf            # 页面字体（说明文字除外，属性筛选按钮也使用）
├── images/
│   ├── attrs/                     # 属性图标（18 个，从 wiki 提取）
│   ├── S1/                        # S1 精灵图片（含 *_异色.png，按第一阶段名称命名）
│   ├── S2/                        # S2 精灵图片（含 *_异色.png）
│   ├── S3/                        # S3 精灵图片（含 *_异色.png）
│   ├── S4/                        # S4 精灵图片（8 只用预览图，文件名带「（预览版）」）
│   ├── high_value/                # 高星光值精灵本体图（46 张，仅最高阶段名称；阶段图已移除）
│   └── star_value.png             # 星光值图标（推荐卡片星光值 tag 用）
├── data/
│   ├── seasons.json               # 赛季列表配置
│   ├── high_value_sprites.json    # 高星光值精灵池（含 stages / excludeSeasons 字段）
│   ├── yise_images.json           # 异色图清单（驱动常规/异色 8s 交替）
│   ├── S1.json                    # S1 赛季数据
│   ├── S2.json                    # S2 赛季数据
│   ├── S3.json                    # S3 赛季数据
│   └── S4.json                    # S4 赛季数据（含定向标记、双分类）
└── README.md
```

> 精灵图片约定：`images/S{赛季}/{精灵名}.png`，放入后页面自动显示；未放入时显示"暂无图片"占位。
